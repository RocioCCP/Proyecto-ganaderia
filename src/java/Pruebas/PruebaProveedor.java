
package Pruebas;

import DaoPersistencia.DaoProveedor;
import static DaoPersistencia.DaoProveedor.grabar;
import Modelo.Proveedor;
import static com.sun.corba.se.spi.presentation.rmi.StubAdapter.request;
import java.util.List;



import Modelo.Proveedor;


public class PruebaProveedor {

    /*
     public static void main(String[] args) {
        // Crear un objeto Proveedor para probar
        Proveedor proveedor = new Proveedor();
        proveedor.setIdProveedor(1); // Asigna el ID del proveedor que deseas actualizar
        proveedor.setCiudadId(2); // Asigna el ID de la ciudad del proveedor
        proveedor.setNomProveedor("Pepito2"); // Asigna el nuevo nombre del proveedor
        proveedor.setTelefono(987654321); // Asigna el nuevo teléfono del proveedor
        proveedor.setCorreo("nuevo@correo.com"); // Asigna el nuevo correo del proveedor
        proveedor.setNomToro("Nuevo Toro"); // Asigna el nuevo nombre del toro

        // Llama al método actualizar y verifica si la actualización fue exitosa
        boolean actualizacionExitosa = DaoProveedor.actualizar(proveedor);

        // Imprime un mensaje según si la actualización fue exitosa o no
        if (actualizacionExitosa) {
            System.out.println("La actualización fue exitosa.");
        } else {
            System.out.println("La actualización falló.");
        }
        }*/
        }
    
    
    /*
 public static void main(String[] args) {
        // Crear un objeto Usuario para probar el método grabar
        
              
           Proveedor proveedor = new Proveedor();

            proveedor.setCiudadId(1);
            proveedor.setNomProveedor("Casablanca");
            proveedor.setTelefono(44444444);
            proveedor.setCorreo("rcf@4gmail.com");
            proveedor.setNomToro("lulo");
            
          // Supongamos que 1 representa un perfil específico
     
// Supongamos que 1 representa un tipo de documento específico
        
// Supongamos que 1 represta un perfil específico
       // usuario.usuario(1); // Supongamos que 1 representa un tipo de documento específico
        
        // Llamar al método grabar para intentar insertar el usuario en la base de datos
        boolean resultado = grabar(proveedor);
        
        // Verificar el resultado e imprimir un mensaje
        if (resultado) {
            System.out.println("El usuario se ha registrado correctamente en la base de datos.");
        } else {
            System.out.println("Error al intentar registrar el usuario en la base de datos.");
        }
        }
        }
        
    */

      
  /*   public static void main(String[] args) {
        List<Proveedor> listaProveedor = DaoProveedor.listar();

        // Imprimir los documentos obtenidos For i
        for (Proveedor proveedor : listaProveedor) {
            System.out.println("ID: " + proveedor.getIdProveedor()
                    + ", Telefoo: " + proveedor.getTelefono()
                    + ", Correo: " + proveedor.getCorreo()
                    + ", toro: " + proveedor.getNomToro()
                    + ", ciudad " +proveedor.getCiudadId());
                        
        }
    }
}*/

     